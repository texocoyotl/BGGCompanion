<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="gridGeneral" parent="">
        <!--<item name="android:layout_marginLeft">@dimen/text_small_margin</item>-->
        <!--<item name="android:layout_marginRight">@dimen/text_small_margin</item>-->
        <!--<item name="android:layout_marginTop">@dimen/text_margin</item>-->
        <!--<item name="android:layout_marginBottom">@dimen/text_margin</item>-->
        <item name="android:layout_margin">@dimen/text_margin</item>
    </style>

    <style name="gridLabel" parent="gridGeneral">
        <item name="android:textStyle">bold</item>
1   </style>

    <style name="gridContent" parent="gridGeneral">

    </style>

</resources>
